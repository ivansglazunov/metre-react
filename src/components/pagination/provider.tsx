import React from "react";

import {
  IStore,
  ISort,
  IColumn,
  IFilter,
  IConfig,
  IStorage,
  IContext,
  IViews,
  IPaginationProps
} from ".";

export default ({ storage, views }: IPaginationProps<any>) => {
  return null;
};
